namespace Camera{
    import ƒ = FudgeCore;
    export class Camera {
        //viewport: ƒ.Viewport;
        //camera: ƒ.Node;

        //multiply cameras? -> than array. + Active cam



        constructor(){

        }
    
    public update():void{
        //update the camera and add the mtxPivot for movement

        //this.camera.mtxPivot.translateX(X);
        //this.camera.mtxPivot.translateY(Y);
        //this.camera.mtxPivot.translateZ(Z);
        //....
    }

    }
}